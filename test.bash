#!/bin/bash
# Test the torrent

# ./taipeitorrent -torrent=testData/a.torrent -fileDir=testData/downloads -port 6881 -useUPnP true
./taipeitorrent -torrent=testData/a.torrent -fileDir=testData/downloads -port 6881
